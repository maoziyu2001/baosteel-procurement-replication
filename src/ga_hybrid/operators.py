"""Genetic operators used by the GA-hybrid solver.

The production implementation lives in ``main.py`` so the reviewer can run a
single self-contained script. This module is kept as a stable import location
for documentation and future tests.
"""

