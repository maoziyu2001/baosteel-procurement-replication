# Example commands

These shell scripts provide one-command entry points for the small complete
case, academic trace generation, ablation experiment, and convergence-related
supplier-count experiment.

